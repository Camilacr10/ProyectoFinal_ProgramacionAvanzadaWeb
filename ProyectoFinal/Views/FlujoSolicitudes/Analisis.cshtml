@{
    ViewData["Title"] = "Análisis de Solicitudes";
    bool puedeAcciones = User.IsInRole("Analista") || User.IsInRole("Admin");
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h3>@ViewData["Title"]</h3>
</div>

<table id="tablaAnalisis" class="table table-striped">
    <thead>
        <tr>
            <th>#</th>
            <th>Cliente</th>
            <th>Monto</th>
            <th>Estado</th>
            <th>Acciones</th>
        </tr>
    </thead>
</table>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    @await Html.PartialAsync("_PermsScript")
    <script>
        window.AppPerms = window.AppPerms || {};
        window.AppPerms.canAnalisisAcciones = @puedeAcciones.ToString().ToLowerInvariant();
    </script>
    <script src="~/js/common.js" asp-append-version="true"></script>
    <script src="~/js/flujo-analisis.js" asp-append-version="true"></script>
}